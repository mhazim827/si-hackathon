# Optional: Flask uses a development value if this is omitted.
SKILLBRIDGE_SECRET_KEY=replace-with-a-long-random-string

# Optional: without these, email content is printed in the server console.
SMTP_EMAIL=your-sending-address@example.com
SMTP_PASSWORD=your-smtp-app-password
